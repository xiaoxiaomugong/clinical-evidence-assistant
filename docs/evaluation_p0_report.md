# P0 离线基线评测报告

**决策：正式验收证据不足；P0 工程回归通过，存在可选依赖跳过与语料质量门禁未达标。**

运行 ID：`20260919T092741Z-p0-1cc225b`。时间：2026-09-19。代码：`1cc225b3e606e516df289daff96883e92b49a57f`。依据：[评测方案](evaluation_plan.md)。本报告由本次机器可读结果生成，未修改产品代码、旧题集、旧评分器或历史报告。

完整工件：[summary.json](../data/eval_runs/20260919T092741Z-p0-1cc225b/summary.json)；[命令与退出码](../data/eval_runs/20260919T092741Z-p0-1cc225b/commands.json)；[环境和依赖](../data/eval_runs/20260919T092741Z-p0-1cc225b/environment.json)；[来源清单](../data/eval_runs/20260919T092741Z-p0-1cc225b/corpus_manifest.json)。工件含本地文献内容，保存在被 Git 忽略的 `data/eval_runs/`，不随仓库公开。

## 环境与范围

- Python 3.9.6，arm64，15 个逻辑 CPU；pytest 8.4.2。性能测试时接交流电；机器型号/内存查询被本机沙箱拒绝，未报告未取得的数据。
- C0：5 个知识页（15 个 claim）+ 10 条文献快照，PDF 路径明确指向不存在的隔离目录。
- C1：C0 + 500 篇本地文献、18,002 个 chunk；480 篇全文、20 篇摘要兜底。SQLite 快照通过 integrity_check，0 个孤立 chunk，500 个 PMID 与 selected_manifest 完全一致。这里只核对清单与索引，未逐份重新审阅 PDF 和医学内容。
- 将 `.env` 指向 `/dev/null`，在清空继承配置的子进程中锁定所有 Settings 字段；LLM/实时 API/云端关闭，凭据为空；本地模型模式开启；缓存独立。
- 请求及实际执行：业务 mode=hybrid，检索 legacy，重排 deterministic；12 题实际回答均为 extractive，3 题前置拒答未运行检索。retrieve_k=40、top_k=8、generation_top_k=5、阈值 0.18/0.5，最少独立来源 3；legacy 分支实际采用各源既有召回配额，未把该参数宣称为已导出的 Top-40。
- Python 网络审计钩子通过 2 次拒绝自检；其余运行未观察到外联尝试。此记录不是 OS 抓包，也不是对全部 PHI 变体逐适配器的完整安全证明。
- 15 题保持原样：12 道旧标签应回答、3 道旧标签应拒答。每个配置完整运行两次；除 elapsed_ms 外全部 PipelineResult 完全一致（15/15）。重复不增加独立样本量。

## 本次旧口径回归

下表为题目宏平均。检索使用原始稀疏二元 qrels（相关文档统一等级 2）；`legacy_rule_*` 仅是内部规则/词项代理，不是独立医学正确率。

| 指标 | C0 | C1 | 分母/说明 |
|---|---:|---:|---|
| 旧 Recall@8 | 75.0% | 75.0% | 12 题，各 2 个相关来源；合计命中 18/24 |
| 旧 MRR | 100.0% | 95.8% | 12 题；旧实现遍历返回条目 |
| 旧 nDCG@8 | 77.6% | 75.5% | 12 题；不与新 0–3 分级口径比较 |
| legacy_rule_citation_accuracy | 100.0% | 100.0% | 12 题宏平均；每配置共 48/48 对通过内部规则 |
| legacy_rule_supported_claim_rate | 100.0% | 100.0% | 12 题宏平均；每配置 48/48 条通过内部规则 |
| legacy_rule_key_point_coverage | 81.7% | 80.0% | 12 题宏平均；词项命中，不是独立语义要点覆盖 |
| 旧应答/拒答行为标签一致率 | 100.0% | 100.0% | 15/15；未独立核验拒答理由 |
| 旧标签应拒答中的拒答比例 | 100.0% | 100.0% | 3/3；未独立核验拒答理由 |
| 旧标签应回答中的误拒答比例 | 0.0% | 0.0% | 0/12；未独立核验拒答理由 |

每配置均有 12/12 道应回答题输出非空回答；0 条陈述被移除。未采集净化前原始文本，不能据此评价删除正确率、生成器原始幻觉率或医学语义安全性。

描述性 95% 区间：旧 Recall@8 两配置均为 62.5%–87.5%；C0/C1 旧 nDCG 分别为 67.8%–87.2% / 64.3%–86.6%；C0/C1 词项覆盖分别为 72.9%–90.0% / 70.8%–89.6%。应答/拒答标签一致率 15/15 的 Wilson 区间为 79.6%–100.0%，3/3 拒答为 43.9%–100.0%，0/12 误拒答为 0.0%–24.2%。其余区间在 summary.json。

宏平均区间以题目 ID 为单位进行 2,000 次 bootstrap（seed=20260919）。旧题缺少独立题组标注，且多题共享来源；这些仅为探索性描述，不能作为计划要求的正式题组区间或验收证据。内部规则均通过时 bootstrap 区间会退化到 100%–100%，不代表临床风险为零。

## 历史数字对齐

| 来源 | Recall@8 | nDCG@8 | 词项覆盖率 | 平均全链路延迟 |
|---|---:|---:|---:|---:|
| 历史 Markdown 报告 | 75.0% | 75.5% | 80.0% | 41.1 ms |
| 历史 JSON 工件 | 100.0% | 96.0% | 89.6% | 53.7 ms |
| 本次 C1 第一次运行 | 75.0% | 75.5% | 80.0% | 39.9 ms |

本次重现了历史 Markdown 的质量指标，未重现旧 JSON 的更高指标。历史工件缺少完整代码/配置指纹，无法仅据这两份文件确定差异产生的全部原因；两份历史文件均原样保留，不混入本次结果。后续比较以本次独立 run_id 为基线。

## 漏检定位与语料问题

| 题目 | 未命中的标注原文 | 实测位置 |
|---|---|---|
| q002、q003 | PMID 31132793 | 进入候选池前存在，build 后被移除 |
| q004、q014 | PMID 39651989 | 进入候选池前存在，build 后被移除 |
| q013 | PMID 34024117 | 进入候选池前存在，build 后被移除 |
| q008 | PMID 29507099 | 重排第 13 位（C0）/第 14 位（C1），未进入 Top-8 |

前 5 题与 `candidate_pool.build` 的研究家族最多保留 2 个条目、优先知识页规则一致：同家族知识页 claim 占据名额，原文快照被丢弃。诊断已记录池前/池后文档 ID，未修改打分或把知识页引用映射改算成原文命中。此问题应在后续开发集中研究“原文检出”和“家族去重”的共同约束。Top-8 也包含同知识页多 claim，不能把 8 个条目解释为 8 篇独立研究。

C1 语料审计：高血压 8 篇、脑卒中 5 篇，未通过每核心主题至少 20 篇的既有语料门禁；399/500（79.8%）证据等级为 Other，未通过 Other 少于 50% 门禁。19 个源文件的已保存 PDF magic 状态无效并使用摘要兜底；未在本次重新检查原始文件。知识页结构 lint 为 0 个问题。

| 旧题主题切片 | 题数 | C0/C1 旧 Recall@8 | 描述性 95% 区间 |
|---|---:|---:|---|
| 高血压 | 3 | 83.3% | 50.0%–100.0% |
| 血脂 | 3 | 66.7% | 50.0%–100.0% |
| 心脑血管 | 4 | 75.0% | 50.0%–100.0% |
| 糖尿病 | 2 | 50.0% | 50.0%–50.0% |
| 脑卒中 | 2 | 75.0% | 50.0%–100.0% |

这些题不是独立盲测，每主题也未满足 ≥12 题；最差主题正式门禁为“证据不足”。

## 工程检查与比较冒烟

- C0：55 项通过、2 项跳过（MCP 依赖、本地 PDF 检查）；C1：56 项通过、1 项跳过（MCP 依赖）。无失败。两次运行共享测试集，不累计为独立测试数。PyMuPDF/SWIG 有 5 条弃用警告，保存在日志。
- C0/C1 的既有 smoke 和安装资源 `desktop --check` 均通过；未验证浏览器界面、MCP stdio、多平台安装或数据库集成。
- A/B/C 各 15 个记录：A 因本轮显式禁用 LLM 而全部 skipped；B 成功 15 次、拒答 3 次；C 既有 degraded 冒烟成功 15 次、全部拒答。旧比较器把 A skipped 记入 errors；summary.json 已明确区分 skipped=15、运行错误=0，同时保留原始结果。C 的拒答比例不是正确率，也不是三种受控扰动的正式结论。
- 额外测试 dense / hybrid / cross_encoder 缺模型时的回退：每个配置 12 道可回答题均按预期标记 degraded，实际回到 legacy+deterministic；3 道前置拒答为 not_run。无模型、无索引，因此不能用于宣称后端质量相同或选出优胜者。

## 全链路耗时

每配置单进程、并发 1，先记录初始化和首请求，再预热 10 次，循环 12 道旧应回答题共测量 120 次（每题 10 次），按 nearest-rank 计算分位数。使用 perf_counter 高精度墙钟时间；未把拒答快路径混入主性能样本。

| 项目 | C0 | C1 |
|---|---:|---:|
| Pipeline 初始化 | 0.47 ms | 0.51 ms |
| 新进程首请求 | 7.36 ms | 46.86 ms |
| P50 | 6.08 ms | 47.79 ms |
| P95 | 6.55 ms | 62.43 ms |
| P99 | 6.84 ms | 62.88 ms |

这是离线抽取式**全链路**时间，不是纯检索+重排时间。线程环境锁为 1，OS 页缓存未清空，首请求不是硬件冷缓存测量；电源策略未控制，未测并发 4、排队、峰值内存和在线成本。本轮不据此宣布正式性能门禁通过。

## 下一阶段与验收状态

P0 已完成本地基线整理、C0/C1 内容快照、旧题/配置/依赖/代码哈希、两次确定性回归、历史数字核对与工程诊断。评测计划全文的 P1–P4 尚未完成：

1. P1：安排两名具备循证训练的评审及一名仲裁者，建立 48 题（16 开发/32 盲测）和独立 qrels/参考要点；盲测由评测负责人单独保存。
2. P2：实现并验证 0–3 分级评分、真正的问题组统计、净化前后陈述、各阶段候选/组件耗时及完整安全故障挑战集；先处理原文候选丢失与语料分布问题。
3. P3/P4：冻结 200 题正式集与候选配置，再开展专家盲评和版本决策。当前最终陈述支持率、数字正确率、安全且有用回答率、危险放行率均未取得独立测量，不能填成内部规则的 100%。

严重安全错误门禁为“未充分评估”，并非“观察到零临床错误即通过”；正式后端切换保持待定。
